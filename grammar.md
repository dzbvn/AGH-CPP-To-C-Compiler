# Grammar


## If
ifstatement := \
`if` (expression) `{`\ 
body `}`\
[ `else` `{`\
body\
`}` ]


## Function 
head := type name ( [params] ) \
params := param { , param } \
param := type name \
type =: `int` 
        | `double` 
        | `char` 
        | `string` 
        | name

## If
